import { Matcher } from './Base';
export declare class NumberCloseToMatcher extends Matcher {
    private readonly target;
    private readonly delta;
    constructor(target: number, delta: number);
    check(v: unknown): boolean;
    toString(): string;
    static make(target: number, { delta }: {
        delta: number;
    }): number;
}
