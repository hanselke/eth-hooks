import { Matcher } from './Base';
export declare class ObjectWithMatcher<T extends Object> extends Matcher {
    private readonly subset;
    constructor(subset: T);
    check(actualItem: unknown): boolean;
    toString(): string;
    static make(subset: Object): any;
}
