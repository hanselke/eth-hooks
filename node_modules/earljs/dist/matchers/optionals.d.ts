import { Matcher } from './Base';
export declare class DefinedMatcher extends Matcher {
    check(actual: unknown): boolean;
    toString(): string;
    static make: () => any;
}
export declare class NullishMatcher extends Matcher {
    check(actual: unknown): boolean;
    toString(): string;
    static make: () => any;
}
