import { TEthersSigner, THookResult, TUpdateOptions } from "../models";
/**
 * #### Summary
 * Are the signers equal and valid
 *
 * @category Hooks
 *
 * @param signer1 Object for first signer to compare
 * @param signer2 Object for second signer to compare
 * @param options Options for how often and when to update
 * @returns
 */
export declare const useAreSignerEqual: (signer1: TEthersSigner | undefined, signer2: TEthersSigner | undefined, options?: TUpdateOptions) => THookResult<boolean | undefined>;
//# sourceMappingURL=useAreSignerEqual.d.ts.map